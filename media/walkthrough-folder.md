```
File › Open Folder...        ~/work/api
                             ~/work/platform
                             ~/side/blog
```

CLI Grid is configured per folder, like `.vscode/settings.json`. Open a folder
the normal way and add an agent — that writes `.vscode/cli-grid.json`, and from
then on opening that folder brings the whole grid back.

A different set of agents is simply a different folder.
