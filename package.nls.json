{
  "cmd.newAgent": "New Agent...",
  "cmd.startAgent": "Start",
  "cmd.startAll": "Start All Agents",
  "cmd.stopAgent": "Stop",
  "cmd.restartAgent": "Restart",
  "cmd.saveAgent": "Save to Project",
  "cmd.removeAgent": "Remove from Project",
  "cmd.openConfig": "Open Project Config",
  "cmd.focusAgent": "Focus Agent",
  "cmd.showAgents": "Show Agents",
  "cmd.refresh": "Refresh",
  "view.agents": "Agents",
  "welcome.noFolder": "Open a folder to get started.\nAgent Grid is configured per folder, like .vscode/settings.json.\n[Open Folder](command:workbench.action.files.openFolder)",
  "welcome.folder": "No agents in this folder yet.\n[New Agent](command:agentGrid.newAgent)\nAdding one writes .vscode/agent-grid.json. Reopen this folder later and the grid comes back.",
  "config.defaultMode": "Whether the launcher starts a CLI fresh or resumes its previous conversation by default. Individual profiles and individual agents can override it. Agent Grid never manages conversation state itself; it only chooses which arguments to pass.",
  "config.defaultMode.new": "Start a fresh conversation",
  "config.defaultMode.resume": "Resume the previous conversation",
  "config.launchStrategy": "`shell` runs your login shell and types the command into it, so CLIs installed via nvm, mise or `~/.local/bin` resolve correctly. `exec` runs the executable directly, which reports the exit code more accurately but requires the command to be on the extension host's `PATH`.",
  "config.launchStrategy.shell": "Run through a login shell (more compatible)",
  "config.launchStrategy.exec": "Execute the command directly (accurate exit codes)",
  "config.autoStart": "Start the folder's agents as soon as it is opened. Off by default so that opening a folder never launches a CLI you did not ask for.",
  "config.revealOnFocus": "When an agent is selected, point the Files view at the folder that agent runs in.",
  "config.profiles": "CLI profiles shown in the launcher. Entries are merged over the built-in defaults for `claude`, `codex` and `gemini`; set `hidden` to `true` to remove one. Arguments are passed through untouched.",
  "walkthrough.title": "Get Started with Agent Grid",
  "walkthrough.description": "Run several coding agents side by side without losing track of which repository each one is in.",
  "walkthrough.step1.title": "Open a folder",
  "walkthrough.step1.description": "Agent Grid is configured per folder. Open one the normal way — there is nothing else to set up.\n[Open Folder](command:workbench.action.files.openFolder)",
  "walkthrough.step2.title": "Add an agent",
  "walkthrough.step2.description": "Pick the folder the CLI should run in, then pick the CLI. The choice is written to .vscode/agent-grid.json so it comes back next time.\n[New Agent](command:agentGrid.newAgent)",
  "walkthrough.step3.title": "Arrange the grid",
  "walkthrough.step3.description": "Pick a split in the Layout section — 2 × 1, 2 × 2, 3 × 2 and so on. The choice is saved with the project.",
  "cmd.initProject": "Make This Folder an Agent Grid Project",
  "welcome.notProject": "Step 1 of 2\n[Make This Folder an Agent Grid Project](command:agentGrid.initProject)\nThis writes .vscode/agent-grid.json. Reopen this folder later and everything comes back.",
  "welcome.noAgents": "Step 2 of 2\n[Add an Agent](command:agentGrid.newAgent)\nPick the folder the CLI runs in, then pick the CLI.",
  "view.files": "Files",
  "view.layout": "Layout",
  "cmd.applyLayout": "Apply Layout",
  "cmd.toggleHiddenFiles": "Show/Hide Hidden Files",
  "config.showHiddenFiles": "Show dotfiles in the Files view. `.vscode` is always shown."
}
